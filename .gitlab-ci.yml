stages:
  - build
  - test

cache:
  key:
    files:
      - package-lock.json
  paths:
    - node_modules/
  policy: pull

install:
  stage: .pre

  cache:
    key: 
      files:
        - package-lock.json
    paths:
    - node_modules/
    when: on_success
    policy: pull-push

  script:
    - npm ci --cache .npm --prefer-offline

build:
  image: node:latest
  stage: build
  script: 
    - npm run build
    # - npm run type:check
    - echo "Build successful"

test:
  stage: test
  needs:
    - job: build
      artifacts: true 
  script: npm test
